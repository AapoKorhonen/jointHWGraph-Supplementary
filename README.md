


# Loggle

R-package Loggle does not work with current R-version. We used R version 4.1.3 to run all Loggle results. Loggle can be installed from https://github.com/jlyang1990/loggle


BiocManager::install("RBGL")
BiocManager::install("Rgraphviz")
